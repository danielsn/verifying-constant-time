sudo: required
dist: trusty
language: c

addons:
    apt:
      sources:
        - ubuntu-toolchain-r-test
      packages:
        - indent
        - cppcheck
        - kwstyle

os:
  - linux

script:
  - gcc --version
  - ruby --version
  - .travis/install_smack.sh
  - mono --version
  - gem install --pre bam-bam-boogieman
  - 
  
